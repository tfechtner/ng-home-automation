{
    "apps": [{
        "name": "nest-home-backend-server",
        "script": "current/backend/dist/main.js",

        "args": "one two",
        "instances": 1,
        "autorestart": true,
        "watch": false,
        "max_memory_restart": "1G",
        "env": {
            "NODE_ENV": "development"
        },
        "env_production": {
            "NODE_ENV": "production"
        }
    }],

    "deploy": {
        "production": {
            "host": ["work-MacMiniNest"],
            "ref": "origin/develop",
            "repo": "git@tfechtner-Github:tfechtner/ng-home-automation.git",
            "path": "/Users/work/Workspace/ng-home-automation",
            "post-deploy": "source ~/.zshrc && cd backend && npm install && npm run prestart:prod && cd .. && pm2 reload ecosystem.json --env production"
        }
    }
}
